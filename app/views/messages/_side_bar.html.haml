.Wrapper
  .Side-bar
    .Side-header
      .Side-list    
        .Side-list__usernames
          = current_user.name
        .Side-list__iconlist
          = link_to new_group_path,class: "Side-list__iconlist" do
            = icon('fas', 'edit', class: "Side-list__edit")
          = link_to edit_user_path(current_user),class: "Side-list__iconlist" do
            = icon('fas', 'cog', class: "Side-list__cog")
          
    .Side-group
      - current_user.groups.each do |group|
        .Group
          = link_to '#' do
            .Side-group__name
              = group.name
            .Side-group__message
              メッセージはまだありません
                  